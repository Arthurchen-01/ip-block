# EgressGuard 反馈报告 —— 来自「完美世界扫号工具」工作区

> 反馈方：`D:\dev_tools\wmrecon\crack`（批量扫号工具）
> 反馈时间：2026-09-23
> 说明：**只反馈，不修改**。请 EgressGuard 工作区自行处理。
>
> 我们不是来挑刺的 —— 这套闸门的核心判据（用 `dwLocalAddr` 而不是路由表判断走没走隧道）
> 是对的，我们直接把它抄进了自己的工具当开工前体检。下面这些是**协同过程中真实踩到的**问题。

---

## 背景：我们和它的关系

我们的工具要联网访问完美世界官网。它跑在 `python.exe` 上（源码形态）
或 `完美世界扫号工具.exe`（打包形态）。

实测确认：**我们的出站连接走隧道**，不是裸奔 ——

```
[tunnel] us.arxlabs.io:3010   本地地址=198.18.0.1   OK 隧道内
```

所以闸门正常工作时不会拦我们。但下面这些问题在协同过程中真实造成了困扰。

---

## 🔴 高优先级（会造成误伤或误导）

### 1. 隔离粒度太粗：按 exe 路径封杀整个程序

**现象**：事件日志里出现

```
"已封杀 C:\Users\s990uma\AppData\Local\Programs\Python\Python312\python.exe 的全部出站流量"
```

**问题**：`python.exe` 是**共用解释器**。这台机器上所有 Python 脚本都叫 `python.exe`。
一个脚本裸奔 → **全部 Python 程序一起断网**，包括完全不相关的工具。

**实际后果**：我们的工具在源码形态下被连带封杀，出现 `WinError 10013` 权限错误，
排查了很久才定位到是闸门干的。

**同类风险**：`java.exe`、`node.exe`、`pythonw.exe`、`powershell.exe`、`cmd.exe` —— 都是共用宿主。

**建议**：
- 隔离单元改成**进程树/命令行**，而不是 exe 路径；
- 或者至少分级：`python.exe` 这类共用宿主默认**只掐连接不隔离程序**，
  隔离前弹一次确认；
- 或者在事件/通知里**明确写清楚**"因为共用宿主，已影响同 exe 的全部程序"。

**注意**：`allow_processes` 白名单同样是按 exe 名匹配的 —— 加 `python.exe` 进白名单
等于放行全机所有 Python。这个粒度问题在配置层面同样存在。

---

### 2. `/api/why` 对**任意 PID** 都返回 `blocked: true`

**现象**：README 说这个接口是给"被掐的程序匿名问自己为什么被掐"用的。
但实测查一个**从未被拦截过**的 PID：

```bash
curl "http://127.0.0.1:47821/api/why?pid=26276"
```

返回：

```json
{
  "blocked": true,
  "code": "DNS_LEAK",
  "reason": "网卡 WLAN 3 配置了非隧道 DNS 192.168.0.1...",
  "process": "本机网络配置", "pid": 0,
  "title": "EgressGuard 已切断：本机网络配置（WLAN 3）（DNS 泄漏）"
}
```

**问题**：返回的是**别人的判定**（pid=0 的 DNS 泄漏），却告诉提问方 `blocked: true`。
一个程序问"我被掐了吗"，会得到"是的，你被掐了，原因是 DNS 泄漏" ——
但它其实**根本没被掐**。

**这是接口语义错误**，会让集成方误判自己该重试还是该改配置。

**建议**：`/api/why?pid=N` 的 `blocked` 必须**只反映该 pid 自己**是否被处置过；
不是它的话返回 `{"blocked": false}`。要查全局状态请用另一个接口。

---

## 🟡 中优先级（影响可用性和排查效率）

### 3. 桌面通知文件内容重复，看不到正文

**现象**：桌面生成的 `EgressGuard_已掐断_python.exe_0923_152503.txt`，
**同一行重复了 15 遍**，正文被挤到很后面（去重后才看到）：

```
==================================================================
  EgressGuard 切断通知 —— 这不是报错，是主动拦截
=
  EgressGuard 切断通知 —— 这不是报错，是主动拦截
=
  EgressGuard 切断通知 —— 这不是报错，是主动拦截
=
  ...（重复 15 次）
```

**问题**：这是**最直观的一条通知通道**（README 里自己写的"最直观"），
但它现在**不可读**。用户第一眼看到的就是 15 行重复的标题。

**建议**：检查通知正文的拼接逻辑，应该是循环/累加写重了。

---

### 4. 两个数据目录，通知里指向的还不是同一个

**现象**：

| 形态 | 数据目录 |
|:--|:--|
| 源码形态 | `D:\工具\EgressGuard\data\` |
| **exe 形态** | `D:\工具\EgressGuard\**dist**\data\` |

而通知文件里写的是：

```
全部事件：
  D:\工具\EgressGuard\dist\data\events.jsonl
```

**实际后果**：我们读 `D:\工具\EgressGuard\data\events.jsonl`，
最后一条是 15:17；而桌面通知是 15:25 生成的 ——
**一度误判成"通知和事件对不上、闸门有 bug"**，翻了半天才发现是读了另一个目录。

**建议**：两种形态统一用 `%LOCALAPPDATA%\EgressGuard\`（和其他 Windows 程序一致），
或者在两个目录都放一个 `README_这里是哪个形态.txt`。

---

### 5. 验收测试会**真加主机路由**、**真隔离 python.exe**，中途崩了会留脏状态

**现象**：README 自己写了测试方法：

```powershell
route add 218.30.118.6 mask 255.255.255.255 192.168.0.1 metric 1 if 24
```

实测这个 IP 出现在真实事件里：

```
15:24:58 [critical] 裸奔出站  python.exe pid=34284  192.168.0.101:58274 -> 218.30.118.6:443
15:25:03 [high]     已隔离程序（封杀 python.exe）
```

**问题**：
- 测试靶子的 IP **硬编码**在文档里，且测试会真改内核路由表、真隔离共用宿主程序
- 清理依赖 `restore_net.ps1` 和脚本正常跑完
- **我们遇到过一次「闸门正在跑验收测试」的窗口期** —— 那几分钟里
  `python.exe` 被真封杀，我们的工具完全没法联网，且不知道是谁干的

**建议**：
- 验收测试期间在**通知/事件里显著标记「这是自测，不是真实泄漏」**；
- 测试靶子 IP 与真实判据区分开（比如用 `203.0.113.0/24` 这类文档保留段）；
- 隔离前先检查目标 exe 是不是共用宿主，是的话跳过或降级为 `notify_only`；
- 崩溃兜底（死亡开关）已有了，建议再加一条**启动时自检残留路由/规则**并自动清理。

---

### 6. DNS 泄漏是**结构性常驻**问题，却按事件反复报

**现象**：`WLAN 3` 配的 DNS 是 `192.168.0.1`（国内），这是**配置事实**，不会自己变。
但事件流里反复出现：

```
15:25:08 [high] DNS_LEAK  已切断：本机网络配置（WLAN 3）（DNS 泄漏）
15:25:57 [high] DNS_LEAK  DNS 泄漏
```

**问题**：它不是"发生了一次泄漏"，而是"一直处于这个状态"。
按事件流报会把真实的一次性事件淹没。

**建议**：区分「状态型判定」和「事件型判定」——
状态型的放在仪表盘的体检条里常驻显示，只在**状态发生变化时**发一条事件。

---

### 7. 守护重启期间 API 完全不可用

**现象**：我们按 README 建议在开工前查一次闸门状态：

```python
requests.get("http://127.0.0.1:47821/api/status", timeout=5)
```

在守护重启的窗口期（实测抓到过），**连接被拒绝**，拿不到任何状态。
此时集成方无法判断"闸门没装"还是"闸门正在重启"。

**建议**：
- API 端口在守护重启时保持监听（返回 `{"booting": true}`）；
- 或者提供第二个只读的探测点。

---

## 🟢 低优先级 / 提一句

### 8. 桌面通知文件会堆积，没有去重或覆盖

跑一段时间的验收后桌面会累积一堆 `EgressGuard_已掐断_*.txt`。
建议同一程序短时间内重复掐断只保留最新一份，或统一收进一个目录。

### 9. `blocked_regions` 里的中文项匹配逻辑不明确

配置里同时有 `"yunnan"/"kunming"` 和 `"云南"/"昆明"`。
不清楚地理库返回的是英文还是中文 —— 如果两个都留着是防御性的没问题，
但建议在注释里写明匹配的是哪个字段。

---

## ✅ 做得好的地方（我们也抄了）

1. **判据选得对**：用 `GetExtendedTcpTable` 的 `dwLocalAddr` 而不是路由表 ——
   这是整个项目最关键的技术事实，我们直接把它搬进了自己的开工前体检
   （`egress_guard.py::check_tunnel_binding`）。
2. **CLOS_WAIT 判成可处置** —— 理由（快泄漏常只能抓到这一帧）说得对。
3. **三档定级**（critical/high/low）而不是两档 —— 把 TIME_WAIT 压到 low 是必须的。
4. **七通道通知**里「控制台红字 + 归属弹窗」这个设计很妙 ——
   视觉上就是那个程序在告诉你原因。
5. **隧道断开熔断**默认开 —— 这才是 kill switch 真正有价值的用法。
6. **诚实标注未通过项**（32 项里那 1 项）—— 不假装解决，这个态度值得学。
7. **README 里那 8 个真 bug 的记录** —— 光看代码发现不了，非常有价值。

---

## 我们的应对（不改你们的代码）

我们不打算把自己的 exe 加进 `allow_processes` —— **白名单等于把保护关掉**，
而我们恰恰要**被它保护**（隧道断了必须被它掐断）。

我们改用的是：**同一个判据自查**。

```
egress_guard.py v2：
  0. 查闸门状态（/api/status）—— 实弹档 + 隧道断 → 拒绝开工
  1. 代理端口没监听 → 拒绝开工（宁可停工，绝不裸连）
  2. ★ 隧道绑定自查：实测连一次代理入口，看本地地址在不在 198.18.0.0/15 → 不在就拒绝
  3. 出口 IP 归属是国内 → 拒绝开工
```

另外：**我们优先用打包好的 exe**（进程名 `完美世界扫号工具.exe`），
这样即使 `python.exe` 被误封也不会连带我们 —— 算是绕开了问题 1，
但**问题 1 本身还是建议修**，因为其他工作区也会踩。

---

## 复现方式（给 EgressGuard 工作区）

```powershell
# 1. 通知文件重复
Get-Content "$env:USERPROFILE\Desktop\EgressGuard_已掐断_python.exe_*.txt" | Select-Object -First 20

# 2. /api/why 语义（需要守护在跑）
curl "http://127.0.0.1:47821/api/why?pid=999999"

# 3. 两个数据目录
Get-Item "D:\工具\EgressGuard\data\events.jsonl","D:\工具\EgressGuard\dist\data\events.jsonl" |
  Select-Object FullName,LastWriteTime

# 4. 隔离粒度
Get-Content "D:\工具\EgressGuard\dist\data\events.jsonl" |
  Select-String "QUARANTINE" | Select-Object -Last 5
```
